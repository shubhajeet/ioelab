int x, y;
while(1) {
         while(!go_i);
         x = x_i;
         y = y_i;
         while(x != y) {
                 if (x < y)
                    y = y - x;
                 else
                    x = x - y;
                 }
         d_o = x;
        }
